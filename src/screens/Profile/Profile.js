import React from 'react';
import {
  SafeAreaView,
  StyleSheet,
  ScrollView,
  View,
  Text,
  StatusBar,
  Button,
} from 'react-native';

const Profile = ({navigation}) => {
  return (
    <SafeAreaView>
      <ScrollView>
        <Text>Halaman Profile</Text>
      </ScrollView>
    </SafeAreaView>
  );
};

export default Profile;
